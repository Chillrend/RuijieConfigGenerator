services:
  ruijie-config-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ruijie-config-generator
    ports:
      - "3001:3001"
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped
    environment:
      - PORT=3001
